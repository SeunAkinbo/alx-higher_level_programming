/* Updates the text color of the <header> element with
   red (#FF0000), using the JQuery API is mandatory.
   Use of document.querySelector is not allowed
*/

$(document).ready(function () {
  $("header").css("color", "#FF0000");
});
